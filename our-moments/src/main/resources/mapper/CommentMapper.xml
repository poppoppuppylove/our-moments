<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gravity.ourmoments.mapper.CommentMapper">

    <resultMap id="CommentResultMap" type="com.gravity.ourmoments.entity.Comment">
        <id property="commentId" column="comment_id"/>
        <result property="postId" column="post_id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="position" column="position"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>

        <!-- Nested Select for relations -->
        <association property="author" column="user_id"
                     select="com.gravity.ourmoments.mapper.UserMapper.findById"/>
    </resultMap>

    <select id="findById" resultMap="CommentResultMap">
        SELECT * FROM blog_comment WHERE comment_id = #{commentId}
    </select>

    <select id="findByPostId" resultMap="CommentResultMap">
        SELECT * FROM blog_comment WHERE post_id = #{postId} ORDER BY create_time ASC
    </select>

    <select id="findByPostIdAndPosition" resultMap="CommentResultMap">
        SELECT * FROM blog_comment WHERE post_id = #{postId} AND position = #{position} ORDER BY create_time ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="commentId">
        INSERT INTO blog_comment (post_id, user_id, content, position, create_time, update_time)
        VALUES (#{postId}, #{userId}, #{content}, #{position}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE blog_comment
        <set>
            <if test="content != null">content = #{content},</if>
            update_time = NOW()
        </set>
        WHERE comment_id = #{commentId}
    </update>

    <delete id="deleteById">
        DELETE FROM blog_comment WHERE comment_id = #{commentId}
    </delete>

    <delete id="deleteByPostId">
        DELETE FROM blog_comment WHERE post_id = #{postId}
    </delete>

</mapper>